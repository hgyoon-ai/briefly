# briefly
Briefly는 브리핑(산업 트렌드 요약)과 마켓(증권사 이벤트)을 자동 수집/요약해 보여주는 서비스입니다.

GitHub Pages: https://hgyoon-ai.github.io/briefly/

## 📁 폴더 구조

```
briefly/
├── src/
│   ├── pages/              # 페이지 컴포넌트
│   │   └── HomePage.jsx
│   ├── components/         # UI 컴포넌트
│   │   ├── TodaysSummary.jsx
│   │   ├── WeeklyTrends.jsx
│   │   └── MonthlyTrends.jsx
│   ├── styles/             # 스타일시트
│   │   ├── index.css
│   │   ├── HomePage.css
│   │   ├── TodaysSummary.css
│   │   ├── WeeklyTrends.css
│   │   └── MonthlyTrends.css
│   └── main.jsx
├── public/
│   ├── briefing/           # 최신 브리핑 데이터 (웹에서 사용)
│   │   ├── ai/
│   │   ├── finance/
│   │   ├── semiconductor/
│   │   └── ev/
│   └── market/             # 마켓 데이터셋 (웹에서 사용)
│       ├── securities-ai/
│       └── securities-updates/
├── archive/                # 브리핑 아카이브(롤업용, 웹 비공개)
├── crawler/                # 크롤링/요약 파이프라인
├── scripts/                # 실행 스크립트
├── index.html
├── package.json
└── vite.config.js
```

## 🚀 실행 방법

### 1. 의존성 설치
```bash
npm install
```

### 1-1. 크롤링 파이프라인 설치 (Python)
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 1-2. 환경 변수 설정
```bash
cp .env.example .env
```

`.env`에 `OPENAI_API_KEY`를 채워주세요.

마켓에서 DART 공시를 쓰는 경우 `DART_API_KEY`도 필요합니다.

### 2. 개발 서버 실행
```bash
npm run dev
```
자동으로 브라우저가 열리고 `http://localhost:5173`에서 확인할 수 있습니다.

### 3. 프로덕션 빌드
```bash
npm run build
```

### 4. 빌드 결과 미리보기
```bash
npm run preview
```

## 🧭 탭/데이터셋

### 산업 탭
- `ai` (AI)
- `finance` (금융/규제)
- `semiconductor` (반도체)
- `ev` (전기차)

### 증권사 데이터셋
- `securities-ai`: 국내 증권사 AI 신호 중심
- `securities-updates`: 국내 증권사 업데이트 신호(AI 제외)

## 🏃 파이프라인 실행 (로컬)

### 산업
- 전체 탭 실행:
```bash
OPENAI_API_KEY=... python3 -m scripts.run_industry_pipeline
```

- 특정 탭만 실행(예: 금융/규제):
```bash
OPENAI_API_KEY=... python3 -m scripts.run_industry_pipeline --tab finance
```

### 증권사
- 데이터셋별 실행(예: 업데이트):
```bash
OPENAI_API_KEY=... python3 -m scripts.run_securities_pipeline --dataset securities-updates
```

## 📱 페이지 구성

### 홈페이지 (HomePage)

**오늘의 요약 섹션:**
- 📝 핵심 내용 (3줄 요약)
- 📰 주요 이슈 카드 (하이퍼링크 포함, 중요도 기반)

**주간 트렌드 섹션:**
- 🏆 주간 Top 토픽 목록 (클릭 시 일별 트렌드 표시)
- ⚠️ 주요 이슈 목록 (클릭 시 관련 기사 모달)
- 📊 선택한 토픽의 일별 트렌드 차트

**월간 트렌드 섹션:**
- ⚠️ 주요 이슈 목록 (클릭 시 관련 기사 모달)
- 📊 선택한 토픽의 월별 추세 차트

### 관리자 페이지 (AdminPage)

- 📊 일간/주간/월간 통계 카드
- 🔎 기준 날짜 및 최근 업데이트 확인
- 🧾 최근 7회 실행 기록: 리스트 + 3줄 스파크라인(브리핑/마켓 동일)

## 💻 기술 스택

- **Framework**: React 18
- **Build Tool**: Vite
- **Styling**: CSS3 (Mobile-First Responsive Design)
- **Crawler**: Python + OpenAI API

## ✨ 특징

✅ 모바일 우선 반응형 디자인
✅ 가벼운 성능 (Vite 번들)
✅ 최신 JSON 데이터로 즉시 테스트 가능

## 🤖 자동 실행 (GitHub Actions)

- `Crawl briefing`: 브리핑 데이터 생성 및 커밋
- `market`: 마켓 데이터셋 생성 및 커밋
- `pages`: GitHub Pages 배포
